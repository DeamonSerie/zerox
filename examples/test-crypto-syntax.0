# Test file for crypto syntax

pub fn main Void world World !
  let message Span<u8> (std.mem.span "Hello")
  mut buf [64]u8 [0_u8; 64]
  let out MutSpan<u8> buf
  let written u32 std.crypto.key.randomBytes 32_u32 out world
  if > written 0_u32
    check world.out.write "test ok\n"
